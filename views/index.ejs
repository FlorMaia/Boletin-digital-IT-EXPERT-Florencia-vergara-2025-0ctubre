<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Colegio Ernesto Guevara</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        .navbar-red {
            background-color: #dc3545 !important;
        }
        .hero-title {
            font-size: 3rem;
            font-weight: bold;
            margin-top: 4rem;
        }
        .description-text {
            font-size: 1.2rem;
            margin: 2rem 0;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }
        .college-image {
            max-width: 100%;
            height: 400px;
            object-fit: cover;
            border-radius: 10px;
            margin: 2rem 0;
        }
        footer {
            background-color: #055488;
            color: white;
            padding: 2rem 0;
            margin-top: 4rem;
        }
    </style>
</head>
<body>
    <!-- Navbar Roja -->
    <nav class="navbar navbar-expand-lg navbar-dark navbar-red">
        <div class="container">
            <!-- Nombre del sitio centrado -->
            <div class="mx-auto">
                <span class="navbar-brand mb-0 h1 fw-bold text-white">Colegio Ernesto Guevara</span>
            </div>
            
            <!-- Menú de navegación a la derecha -->
            <div class="navbar-nav ms-auto">
                <a class="nav-link text-white" href="/">Inicio</a>
                <% if (locals.isAuthenticated && locals.isStudent) { %>
                    <a class="nav-link text-white" href="/dashboard">Boletín de Notas</a>
                <% } else { %>
                    <a class="nav-link text-white" href="/dashboard">Boletín de Notas</a>
                <% } %>
                <% if (!locals.isAuthenticated) { %>
                    <a class="nav-link text-white" href="/login">Iniciar Sesión</a>
                <% } else { %>
                    <a class="nav-link text-white" href="/logout">Cerrar Sesión</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Contenido Principal -->
    <div class="container">
        <!-- Título Principal -->
        <div class="text-center">
            <h1 class="hero-title">Bienvenidos al Colegio Ernesto Guevara</h1>
            
            <!-- Texto Descriptivo -->
            <p class="description-text">
                En el Colegio Ernesto Guevara, nos dedicamos a ofrecer una educación de calidad que fomente el desarrollo integral de nuestros estudiantes.
            </p>
            
            <!-- Imagen del Colegio -->
            <img src="/colegio-ernesto-guevara.jpg" 
                 alt="Colegio Ernesto Guevara" 
                 class="college-image img-fluid">
        </div>
        
        <!-- Botones de acceso rápido -->
        <% if (!locals.isAuthenticated) { %>
            <div class="text-center mt-4">
                <a href="/login" class="btn btn-danger btn-lg me-3">
                    <i class="bi bi-box-arrow-in-right"></i> Iniciar Sesión
                </a>
                <a href="/register" class="btn btn-outline-danger btn-lg">
                    <i class="bi bi-person-plus"></i> Registrarse
                </a>
            </div>
        <% } else { %>
            <div class="text-center mt-4">
                <a href="/dashboard" class="btn btn-danger btn-lg">
                    <i class="bi bi-speedometer2"></i> Ir al Dashboard
                </a>
            </div>
        <% } %>
    </div>

    <!-- Pie de Página -->
    <footer>
        <div class="container text-center">
            <p class="mb-0">&copy; <%= new Date().getFullYear() %> Colegio Ernesto Guevara. Todos los derechos reservados.</p>
            <p class="mb-0">Formando el futuro de nuestros estudiantes</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 