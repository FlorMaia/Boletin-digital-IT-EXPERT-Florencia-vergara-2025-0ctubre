<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Estudiante - Colegio Ernesto Guevara</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        .navbar-red { background: linear-gradient(135deg, #8B0000, #dc3545) !important; }
        .main-title { color: #333; font-weight: bold; }
        .year-card {
            transition: transform 0.2s, box-shadow 0.2s;
            text-decoration: none;
            color: inherit;
        }
        .year-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 4px 15px rgba(0,0,0,0.15);
        }
        footer { background-color: #272530; color: white; padding: 2rem 0; margin-top: 4rem; }
    </style>
</head>
<body class="bg-light">
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark navbar-red">
        <div class="container">
            <div class="d-flex align-items-center">
                <i class="bi bi-mortarboard text-white me-2 fs-4"></i>
                <span class="navbar-brand mb-0 h4 fw-bold text-white">Aula Digital</span>
            </div>
            <div class="navbar-nav ms-auto d-flex flex-row align-items-center">
                <span class="text-white me-3">
                    <i class="bi bi-person-circle"></i>
                    <%= user.first_name %> <%= user.last_name %>
                </span>
                <a class="nav-link text-white fw-bold" href="/logout">
                    <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
                </a>
            </div>
        </div>
    </nav>

    <!-- Contenido Principal -->
    <div class="container mt-4">
        <div class="text-center mb-5">
            <h1 class="main-title">Mi Progreso Académico</h1>
            <p class="text-muted fs-5">Selecciona un año lectivo para ver tu boletín detallado.</p>
        </div>

        <% if (años && años.length > 0) { %>
            <div class="row justify-content-center">
                <% años.forEach(ano => { %>
                    <div class="col-md-4 col-lg-3 mb-4">
                        <a href="/dashboard/boletin/<%= ano %>" class="card text-center year-card h-100">
                            <div class="card-body d-flex flex-column justify-content-center">
                                <i class="bi bi-calendar-check-fill fs-1 text-danger mb-2"></i>
                                <h4 class="card-title">Año <%= ano %></h4>
                                <p class="card-text">Ver boletín</p>
                            </div>
                        </a>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="alert alert-info text-center">
                <i class="bi bi-info-circle fs-1 mb-3"></i>
                <h4>Sin Historial</h4>
                <p>Aún no tienes un historial académico para mostrar.</p>
            </div>
        <% } %>
    </div>

    <!-- Pie de Página -->
    <footer>
        <div class="container text-center">
            <p class="mb-0">&copy; <%= new Date().getFullYear() %> Colegio Ernesto Guevara. Todos los derechos reservados.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 