<nav class="navbar navbar-expand-lg navbar-dark navbar-red">
    <div class="container-fluid">
        <div class="d-flex align-items-center">
            <i class="bi bi-shield-check text-white me-2 fs-4"></i>
            <span class="navbar-brand mb-0 h4 fw-bold text-white">Panel de Administración</span>
        </div>
        
        <div class="navbar-nav ms-auto">
            <span class="nav-link text-white me-3">
                <i class="bi bi-person-circle"></i> <%= user.first_name %> <%= user.last_name %>
            </span>
            <a class="nav-link text-white fw-bold" href="/logout">
                <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
            </a>
        </div>
    </div>
</nav> 